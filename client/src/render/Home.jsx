import React from "react";
import Display from "../component/display";
import Form from "../component/form";

const Home = (props) => {
        return (
        <div>
            <Form/>
            <hr></hr>
            <Display/>
        </div>
    );
};

export default Home;